name: Release Wireless Management Extension

on:
  push:
    tags:
      - 'wlman/v*'

jobs:
  release:
    runs-on: windows-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build Wireless Management Extension
        working-directory: extensions/wlman
        shell: pwsh
        run: ./build.ps1

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: extensions/wlman/dist/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
